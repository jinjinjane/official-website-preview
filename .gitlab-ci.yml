image: node:20

stages:
  - deploy

pages:
  stage: deploy
  tags:
    - shell
  script:
    - npm ci
    - npm run build
    - rm -rf public
    - mv dist public
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
